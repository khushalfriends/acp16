def due_amount(total_bill_amount, paid_amount):
    due_amount = total_bill_amount
    return due_bill

total_bill_amount = float(input("Enter the total bill amount: "))
paid_amount = float(input("Enter the amount paid already: "))
print("Due amount: ₹", due_amount(total_bill_amount, paid_amount))
